generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// NOTE: Existing schema content is intentionally not rewritten here.
// This update was blocked from full replacement by the connector previously.
// ConversionEvent is managed through raw Mongo indexes until the full schema can be patched safely.
